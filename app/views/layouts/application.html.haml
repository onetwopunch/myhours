!!!
%html
  %head
    %title MyHours
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    = favicon_link_tag 'favicon.ico'
  %body{style: "padding-top: 70px"}
    .navbar.navbar-inverse.navbar-fixed-top{role: "navigation"}
      .container
        .navbar-header
          = link_to(image_tag(image_path("myhourslogo.png"), height: '50'), |
            {:controller => 'profile', :action => 'index'})               |
        .navbar-collapse.collapse
          .navbar-form.navbar-right
            - if session[:user_id]
              = link_to('Log Out', {:controller => 'login', :action => 'logout'}, :class => 'btn btn-danger')
            - else
              = link_to('Log In', {:controller => 'login', :action => 'index'}, :class => 'btn btn-success')
    - if (params[:action] == 'signup')
      .banner
        %h3 Have you ever thought, "Why should I pay money to track my MFT hours when I'm not getting paid yet?"
        %h1 Sign up to track your MFT hours for free!
    .container
      .buffer
      = yield